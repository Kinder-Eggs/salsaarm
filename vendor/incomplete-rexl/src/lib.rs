#![allow(dead_code)]

// Local compatibility stub for the optional incomplete-rexl backend.
// This project does not currently use the crate directly; the goal is to keep the
// feature gate available without pulling in the upstream AVX-512 implementation
// that fails to compile on the current toolchain.
